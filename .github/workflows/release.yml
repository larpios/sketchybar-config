name: Release

permissions:
  contents: write

on:
  push:
    tags:
      - 'v*'

jobs:
  create-release:
    runs-on: ubuntu-latest
    outputs:
      release_id: ${{ steps.create_release.id }}
    steps:
      - uses: actions/checkout@v4
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  upload-assets:
    name: Build and Upload Assets
    needs: create-release
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin, aarch64-apple-darwin

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2

      - name: Build x86_64
        run: cargo build --release --target x86_64-apple-darwin

      - name: Build aarch64
        run: cargo build --release --target aarch64-apple-darwin

      - name: Create Universal Binary
        run: |
          lipo -create -output sketchybarrc \
            target/x86_64-apple-darwin/release/sketchybarrc \
            target/aarch64-apple-darwin/release/sketchybarrc
          tar -czvf sketchybarrc-universal-apple-darwin.tar.gz sketchybarrc
          
          # Also package individual ones just in case
          cp target/x86_64-apple-darwin/release/sketchybarrc sketchybarrc-x86_64
          tar -czvf sketchybarrc-x86_64-apple-darwin.tar.gz sketchybarrc-x86_64
          
          cp target/aarch64-apple-darwin/release/sketchybarrc sketchybarrc-aarch64
          tar -czvf sketchybarrc-aarch64-apple-darwin.tar.gz sketchybarrc-aarch64

      - name: Upload Binaries
        uses: softprops/action-gh-release@v2
        with:
          files: |
            sketchybarrc-universal-apple-darwin.tar.gz
            sketchybarrc-x86_64-apple-darwin.tar.gz
            sketchybarrc-aarch64-apple-darwin.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
